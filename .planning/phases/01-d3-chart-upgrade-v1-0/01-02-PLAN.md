---
phase: 01-d3-chart-upgrade-v1-0
plan: 01-02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - app/components/line-chart.vue
autonomous: true
requirements:
  - CHART-02
user_setup: []
must_haves:
  truths:
    - "A transparent `rect` over the plot area captures pointer move/leave (nearest-x model)."
    - "Horizontal movement anywhere over the plot highlights exactly one logical year (nearest in x) with tooltip + vertical rule; leaving the plot clears crosshair, highlight, and tooltip."
  artifacts:
    - path: "app/components/line-chart.vue"
      provides: "plotOverlay, bisect/bisector, hoverLine, highlightDot, tooltipG"
  key_links:
    - from: "plotOverlay pointer events"
      to: "d.pointers + xScale.invert + bisect"
      via: "showTooltip / hideTooltip"
      pattern: "handlePlotPointerMove|handlePlotPointerLeave"
---

<objective>
Lock the nearest-x interaction model: bisector-based year selection, full-height crosshair, single highlighted point on hover, and clean teardown on pointer leave — matching ROADMAP Phase 1 success criterion 2 and 01-CONTEXT.

Purpose: Delivers CHART-02; builds on plan 01-01 line geometry so tooltip/highlight use the same `xScale`/`yScale`+noise as the line.

Output: Updated pointer handlers and DOM selections in `line-chart.vue` only; `story-page.vue` still passes only `data` + domains.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-d3-chart-upgrade-v1-0/01-CONTEXT.md
@/Users/guillem/vault/projects/personal/ib-gdp-evolution/app/components/line-chart.vue
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Full-height crosshair and nearest-year bisect</name>
  <files>app/components/line-chart.vue</files>
  <action>
Use **`d3.bisector` / `bisect.center`** (or equivalent) on a **deduped, sorted** year list so the highlighted row is the **nearest** calendar year in **data space** to the pointer’s inverted x (per ROADMAP “nearest year”). **Crosshair (CHART-02 / CONTEXT):** draw a vertical line spanning the **full plot height** — i.e. from `y = MARGIN.top` to `y = HEIGHT - MARGIN.bottom` (not only from the data point down). The **highlight dot** stays at the interpolated position on the line: `(xScale(year), yScale(gdp) + noiseOffset)` consistent with `showTooltip`. Tolerance: if pointer maps outside `[min year, max year]`, clamp to the nearest end year (or hide — pick one policy and document in code with minimal comment if non-obvious; prefer matching current UX if already good).
  </action>
  <verify>
    <automated>cd /Users/guillem/vault/projects/personal/ib-gdp-evolution &amp;&amp; pnpm run build</automated>
  </verify>
  <done>Crosshair is full plot height; bisect always picks one nearest data year for in-range interaction.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Pointer leave and tooltip parity</name>
  <files>app/components/line-chart.vue</files>
  <action>
On **`pointerleave`** (and `pointercancel` if needed), call **`hideTooltip()`** so **crosshair, highlight dot, and tooltip group** are all hidden. Ensure the overlay `rect` remains `pointer-events: all` and sits above the series so move events are reliable. **Do not** reintroduce per-point `pointer-events` on circles for the default view (CHART-01). Tooltip text continues to use `valueFormat` and source label; position stays clamped to margins.
  </action>
  <verify>
    <automated>cd /Users/guillem/vault/projects/personal/ib-gdp-evolution &amp;&amp; pnpm run build</automated>
  </verify>
  <done>Leaving the plot area clears all hover affordances; no orphan tooltip/crosshair.</done>
</task>

</tasks>

<threat_model>
| ID | Category | Component | Disposition | Mitigation |
|----|----------|-----------|-------------|------------|
| T-01-02-01 | Spoofing | N/A | accept | no auth |
| T-01-02-02 | DoS | rapid pointermove | mitigate | O(log n) bisect, no layout thrash |
</threat_model>

<verification>
- Move cursor across width: one year at a time, tooltip values match CSV for that year.
- Crosshair hits top and bottom guide edges of the plot.
- Exiting the SVG plot: UI clears.
</verification>

<success_criteria>
CHART-02 met: nearest-x, full-height crosshair, single highlight, clean leave; build passes.
</success_criteria>

<output>
After completion, create `.planning/phases/01-d3-chart-upgrade-v1-0/01-02-SUMMARY.md`.
</output>
