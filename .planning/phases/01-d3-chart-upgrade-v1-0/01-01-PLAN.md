---
phase: 01-d3-chart-upgrade-v1-0
plan: 01-01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/line-chart.vue
autonomous: true
requirements:
  - CHART-01
  - CHART-03
  - CHART-05
user_setup: []
must_haves:
  truths:
    - "Default chart state shows no visible data-point circles (r=0 or equivalent)."
    - "Main series line uses smooth simplex-based vertical offsets that read organic, not random jitter (per 01-CONTEXT.md)."
    - "A visible arrow cap sits at the latest-year end of the line, color-matched to the stroke, implemented via shared SVG marker defs and marker-end (per 01-CONTEXT.md)."
  artifacts:
    - path: "app/components/line-chart.vue"
      provides: "Hidden default points, resampled+noise line path, defs/marker for line endcap"
      contains: "marker-end|showAllPoints|buildResampledSeries|simplex"
  key_links:
    - from: "path.series-line"
      to: "SVG <defs> marker"
      via: "marker-end on primary line path"
      pattern: "marker-end|ref\\(arrow"
---

<objective>
Finalize editorial defaults for Act I: no always-on dots, stable simplex-based organic line geometry, and a standard SVG `marker` end cap tied to the line stroke — replacing or consolidating any ad-hoc arrow geometry so the terminator stays correct through path updates and animations.

Purpose: Meets CHART-01, CHART-03, and CHART-05 with the interaction model in CONTEXT (deterministic noise, endcap from defs).

Output: `line-chart.vue` with marker-based endcap, unchanged `GdpDataPoint` export, `story-page.vue` contract unchanged.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-d3-chart-upgrade-v1-0/01-CONTEXT.md
@/Users/guillem/vault/projects/personal/ib-gdp-evolution/CLAUDE.md

<interfaces>
From `app/components/line-chart.vue` (excerpt — executor reads full file):
```typescript
export interface GdpDataPoint {
  year: number;
  gdp_pc: number;
  source: string;
  unit?: string;
}
```
Props: `data`, `comparisonData?`, `yDomain?`, `xDomain?`, `showNoiseControls?`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: CHART-01 default hidden data points</name>
  <files>app/components/line-chart.vue</files>
  <action>
Ensure the **default** rendered state has **no visible** series circles: `r` 0 (or `display: none`) for all `pointsG` circles when `showAllPoints` is false (per 01-CONTEXT: hidden default points). Do not add new user-facing toggles beyond existing `showNoiseControls` + `showAllPoints` if those remain for dev/QA. Remove or strictly gate any debug-only visual that would violate CHART-01 in production. Match editorial palette for stroke/fill when `showAllPoints` is true for QA only.
  </action>
  <verify>
    <automated>cd /Users/guillem/vault/projects/personal/ib-gdp-evolution &amp;&amp; pnpm run build</automated>
  </verify>
  <done>With `showAllPoints` false, no year dots are visible in the static SVG before pointer interaction; TypeScript/IDE clean for edited file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CHART-03 deterministic simplex + bounded work</name>
  <files>app/components/line-chart.vue</files>
  <behavior>
    - For the same input rows and noise key, y-offsets at each resampled `year` are stable across re-renders and `update(false)` (no unbounded re-randomization).
    - Resampling step + curve alpha keep the path hand-drawn but readable (not high-frequency jitter).
  </behavior>
  <action>
Keep `simplex-noise` + `alea` (or equivalent) with keys including source/year per CONTEXT. **Do not** move Perlin work into per-frame scroll handlers; cache per series revision (`ensureNoiseCache` / `buildNoiseKey` pattern) so scrollama steps stay smooth (CLAUDE.md performance constraint). Fine-tune `DEFAULT_*` constants only as needed; avoid increasing work per `update()` beyond what the current resampled series already does.
  </action>
  <verify>
    <automated>cd /Users/guillem/vault/projects/personal/ib-gdp-evolution &amp;&amp; pnpm run build</automated>
  </verify>
  <done>Build passes; simplex path remains coherent in code review; no new hot loops in pointer handlers.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: CHART-05 SVG marker defs and marker-end</name>
  <files>app/components/line-chart.vue</files>
  <action>
Per 01-CONTEXT.md: add a single (or per-source) `<marker>` in `<defs>` and attach **`marker-end`** to the **primary** `path.series-line` (stroke color `#660000` / `LINE_COLOR` / `sourceColor` as required). **Refactor** the existing free-form `arrowPath` triangle so the visible end cap is the marker: either (a) remove `arrowPath` and rely on `marker-end` for static end state, and update domain/zoom/erase animations to preserve correct tip orientation, or (b) keep a minimal overlay only where `marker-end` cannot express an animation requirement — **prefer (a)** and use path `d` updates + `marker-end` so the cap tracks the line tip. If zoom tween temporarily uses custom tip placement, end each tween with `path` + marker in a consistent DOM state. Do not break `comparisonData` ghost lines (no marker required there unless specified).
  </action>
  <verify>
    <automated>cd /Users/guillem/vault/projects/personal/ib-gdp-evolution &amp;&amp; pnpm run build</automated>
  </verify>
  <done>Arrow cap at latest year uses defs-based marker; visual matches line stroke; `pnpm run build` succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Browser → D3 | Pointer coordinates are local; no network exfil. |

## STRIDE (disposition)
| ID | Category | Component | Disposition | Mitigation |
|----|----------|-----------|-------------|------------|
| T-01-01-01 | Elevation of privilege | N/A (static) | accept | — |
| T-01-01-02 | Denial of service | heavy noise in `update` | mitigate | keep caching; avoid O(n²) in pointer move |
| T-01-01-03 | Tampering | CSV via `useFetch` | accept | out of phase scope (Act I data trusted) |
</threat_model>

<verification>
- **CHART-01:** Visual: load `/`, no hover — no visible dots.
- **CHART-03:** No jitter storm; same year → same offset after refresh (deterministic seed path).
- **CHART-05:** Inspect SVG: `<defs><marker` and `marker-end` on main line; arrow color matches stroke.
</verification>

<success_criteria>
CHART-01, CHART-03, and CHART-05 satisfied with `story-page.vue` API unchanged; `pnpm run build` green.
</success_criteria>

<output>
After completion, create `.planning/phases/01-d3-chart-upgrade-v1-0/01-01-SUMMARY.md` per GSD template.
</output>
