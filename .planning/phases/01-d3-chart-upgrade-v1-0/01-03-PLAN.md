---
phase: 01-d3-chart-upgrade-v1-0
plan: 01-03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - app/components/line-chart.vue
autonomous: false
requirements:
  - CHART-04
  - CHART-06
user_setup: []
must_haves:
  truths:
    - "On first paint and on scroll-driven `update(true)` (same-domain growth / standard path), the main line reads as a progressive draw (stroke-dash technique) with timing aligned to project spec: **800ms** base duration per 01-CONTEXT / ROADMAP unless a longer tween is required for domain-zoom (document which branch uses 800ms)."
    - "`pnpm run generate` completes with exit code 0."
    - "No runtime `console.*` calls ship in `line-chart.vue` (and no errors during story scroll in manual check)."
  artifacts:
    - path: "app/components/line-chart.vue"
      provides: "applyLineGrow / transition wiring for mount and watch-driven update"
  key_links:
    - from: "watch props → update(true)"
      to: "path.series-line stroke-dash animation"
      via: "applyLineGrow"
      pattern: "update\\(true\\)|applyLineGrow"
---

<objective>
Align line “draw-on” motion with the 800ms editorial cadence for normal reveals, preserve coordinated axis transitions from existing logic, and verify static generation plus browser sanity for scrollytelling (CHART-04 + CHART-06).

Purpose: Closes the motion + ship gates for v1.0 Act I chart.

Output: Tuned `applyLineGrow` / `update` paths; verified `nuxt generate`; optional devtools confirmation logged in SUMMARY, not in app code.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md (Phase 1 success criteria 4, 6)
@.planning/phases/01-d3-chart-upgrade-v1-0/01-CONTEXT.md (Motion: 800ms)
@/Users/guillem/vault/projects/personal/ib-gdp-evolution/nuxt.config.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: CHART-04 stroke-dash grow at 800ms (standard path)</name>
  <files>app/components/line-chart.vue</files>
  <action>
Per 01-CONTEXT: animate line reveal with **`stroke-dasharray` / `stroke-dashoffset`**. Set **default duration to 800ms** for the **primary** “new data forward” and **initial mount** reveals where the roadmap requires the drawn line effect. Retain `MAX_DURATION` / `MIN_DURATION` only for **legitimate** long tweens (e.g. wide domain-width zoom) or backward erase — if those must differ, keep them but ensure the **common** scroll step (same x-domain, year range extends forward) uses **800ms** cubic easing (`d3.easeCubicInOut`) to match the narrative spec. Synchronize `xAxisG` transition duration with the line animation duration **for the same branch** so the axis and line do not drift visually. If plan 01-01 moved the arrow to `marker-end`, ensure grow tweens still end with the marker visible at the path tip.
  </action>
  <verify>
    <automated>cd /Users/guillem/vault/projects/personal/ib-gdp-evolution &amp;&amp; pnpm run build</automated>
  </verify>
  <done>Standard `update(true)` forward growth uses 800ms (or explicitly documented exception list in SUMMARY only — code should make the default obvious via named constant e.g. `REVEAL_MS = 800`).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: CHART-06 static generation</name>
  <files>app/components/line-chart.vue</files>
  <action>
Run **`pnpm run generate`** from the project root. Fix any SSG issues caused by `line-chart.vue` (e.g. `window` access without guard — should already be in `onMounted` / D3). Ensure **no** `console.log` / `console.error` added. Confirm `simplex-noise` / `d3` work in client bundle only as today.
  </action>
  <verify>
    <automated>cd /Users/guillem/vault/projects/personal/ib-gdp-evolution &amp;&amp; pnpm run generate</automated>
  </verify>
  <done>`pnpm run generate` exits 0; output directory produced.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: CHART-06 browser smoke (console + scroll)</name>
  <files>app/components/line-chart.vue</files>
  <action>
Executor makes no code changes in this task. Human confirms CHART-06: no console errors during Act I scrollytelling and that line draw timing feels correct after Tasks 1–2 of this plan.
  </action>
  <verify>
    <automated>cd /Users/guillem/vault/projects/personal/ib-gdp-evolution &amp;&amp; pnpm run generate</automated>
  </verify>
  <done>Human reports `approved` (or lists blocking issues). Automated `pnpm run generate` re-passes as sanity before sign-off.</done>
  <what-built>Act I line chart after plans 01-01 and 01-02, with motion from Task 1 and static build from Task 2.</what-built>
  <how-to-verify>
1. `pnpm dev`, open `http://localhost:3000`, open devtools **Console** (filter errors).
2. Scroll through the full Act I scrollytelling: advance several steps, move pointer over the chart, leave the chart, scroll back. Confirm no red console errors and no unhandled rejections.
3. Confirm the line animates in on first view and on step changes that extend the series (progressive draw, about 800ms for standard path).
4. (Optional) Run `pnpm preview` on generated output and repeat a shorter console check.
  </how-to-verify>
  <resume-signal>Reply `approved` or list console errors or UX issues to fix in a follow-up plan.</resume-signal>
</task>

</tasks>

<threat_model>
| ID | Category | Component | Disposition | Mitigation |
|----|----------|-----------|-------------|------------|
| T-01-03-01 | I | nuxt generate | accept | SSG, no user data at build time |
| T-01-03-02 | I | devtools in CI | accept | no secrets in client |
</threat_model>

<verification>
| Criterion | Automated | Manual |
|-----------|-----------|--------|
| CHART-04 | `pnpm run build` | Observe draw timing in browser |
| CHART-06 | `pnpm run generate` | DevTools console clean on scroll |
</verification>

<success_criteria>
CHART-04 and CHART-06 satisfied: 800ms default reveal, generate passes, user confirms no console errors during interaction.
</success_criteria>

<output>
After completion, create `.planning/phases/01-d3-chart-upgrade-v1-0/01-03-SUMMARY.md` recording exact durations used per code branch and any ROADMAP exceptions.
</output>
