---
phase: 05-data-lake-ingestion-catalog-index
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/datalake_eurostat_index.json
autonomous: true
requirements:
  - INGEST-01
  - INGEST-02
  - INGEST-03

must_haves:
  truths:
    - "Data-lake contains Eurostat nama_10r_2gdp EUR_HAB for ES43 and ES61"
    - "Data-lake contains Eurostat nama_10_pc CLV10_EUR_HAB for all 15 EU-15 geos + Act2 geos"
    - "Data-lake contains Eurostat demo_pjan population for all 15 EU-15 geos"
    - "Static JSON index maps every (dataset|unit|geo_key) triple to a data-lake source_id"
  artifacts:
    - path: "scripts/datalake_eurostat_index.json"
      provides: "Deterministic lookup from (dataset, unit, geos) to data-lake source IDs"
      contains: "nama_10r_2gdp|EUR_HAB|ES53"
  key_links:
    - from: "scripts/datalake_eurostat_index.json"
      to: "/Users/guillem/vault/projects/personal/data-lake/lake/sources/<id>/raw.json"
      via: "source_id field resolves to artifact path"
      pattern: "lake/sources/.*/raw\\.json"
---

<objective>
Ingest all required Eurostat datasets into the data-lake and build a static catalog index file.

Purpose: Provide local, reproducible Eurostat JSON-stat v2.0 artifacts that the ETL adapter (Plan 02) will read instead of hitting the live API.
Output: 3 new data-lake sources + `scripts/datalake_eurostat_index.json` mapping file.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-data-lake-ingestion-catalog-index/05-CONTEXT.md
@.planning/phases/05-data-lake-ingestion-catalog-index/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ingest 3 Eurostat API sources into data-lake</name>
  <files>(external: /Users/guillem/vault/projects/personal/data-lake/)</files>
  <read_first>
    - /Users/guillem/vault/projects/personal/data-lake/lake/sources/01KPWPQ902XYG9BT12QCXKFG4K/meta.yaml (existing ES53 source — verify artifact structure)
  </read_first>
  <action>
Run the data-lake CLI to ingest 3 new Eurostat sources. The data-lake project is at `/Users/guillem/vault/projects/personal/data-lake/`. Use `uv run python -m data_lake.ingest.cli.app ingest <url> --format json` for each.

**Source 1: nama_10r_2gdp ES43 (Extremadura NUTS2 regional GDP EUR_HAB)**
```
cd /Users/guillem/vault/projects/personal/data-lake
uv run python -m data_lake.ingest.cli.app ingest \
  "https://ec.europa.eu/eurostat/api/dissemination/statistics/1.0/data/nama_10r_2gdp?format=JSON&lang=en&unit=EUR_HAB&geo=ES43" \
  --format json
```
Record the returned source_id.

**Source 2: nama_10r_2gdp ES61 (Andalucia NUTS2 regional GDP EUR_HAB)**
```
cd /Users/guillem/vault/projects/personal/data-lake
uv run python -m data_lake.ingest.cli.app ingest \
  "https://ec.europa.eu/eurostat/api/dissemination/statistics/1.0/data/nama_10r_2gdp?format=JSON&lang=en&unit=EUR_HAB&geo=ES61" \
  --format json
```
Record the returned source_id.

**Source 3: nama_10_pc CLV10_EUR_HAB for all EU-15 + Act2 geos (batched)**
```
cd /Users/guillem/vault/projects/personal/data-lake
uv run python -m data_lake.ingest.cli.app ingest \
  "https://ec.europa.eu/eurostat/api/dissemination/statistics/1.0/data/nama_10_pc?format=JSON&lang=en&unit=CLV10_EUR_HAB&na_item=B1GQ&geo=AT&geo=BE&geo=DK&geo=FI&geo=FR&geo=DE&geo=EL&geo=IE&geo=IT&geo=LU&geo=NL&geo=PT&geo=ES&geo=SE&geo=UK" \
  --format json
```
Record the returned source_id.

**Source 4: demo_pjan population for all EU-15 geos (batched)**
```
cd /Users/guillem/vault/projects/personal/data-lake
uv run python -m data_lake.ingest.cli.app ingest \
  "https://ec.europa.eu/eurostat/api/dissemination/statistics/1.0/data/demo_pjan?format=JSON&lang=en&sex=T&age=TOTAL&geo=AT&geo=BE&geo=DK&geo=FI&geo=FR&geo=DE&geo=EL&geo=IE&geo=IT&geo=LU&geo=NL&geo=PT&geo=ES&geo=SE&geo=UK" \
  --format json
```
Record the returned source_id.

After ingestion, verify each source has a valid `raw.json`:
```
ls -la /Users/guillem/vault/projects/personal/data-lake/lake/sources/<source_id>/raw.json
```

Note: Source for ES53 already exists with ID `01KPWPQ902XYG9BT12QCXKFG4K`. Do NOT re-ingest it.
  </action>
  <verify>
    <automated>test -f /Users/guillem/vault/projects/personal/data-lake/lake/sources/01KPWPQ902XYG9BT12QCXKFG4K/raw.json && echo "ES53 exists" && ls /Users/guillem/vault/projects/personal/data-lake/lake/sources/*/raw.json | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - 4 total data-lake source directories exist under `lake/sources/` with `raw.json` files: ES53 (existing), ES43, ES61, national batch, population batch
    - Each `raw.json` is valid JSON containing `"id"` and `"dimension"` keys (JSON-stat v2.0 markers)
    - CLI output for each ingestion shows `source_id=<ULID>` — record these IDs for Task 2
  </acceptance_criteria>
  <done>4 data-lake sources contain valid Eurostat JSON-stat v2.0 raw.json artifacts (1 pre-existing + 3 newly ingested)</done>
</task>

<task type="auto">
  <name>Task 2: Create static catalog index file</name>
  <files>scripts/datalake_eurostat_index.json</files>
  <read_first>
    - /Users/guillem/vault/projects/personal/data-lake/lake/sources/01KPWPQ902XYG9BT12QCXKFG4K/meta.yaml (verify source_id format and structure)
  </read_first>
  <action>
Create `scripts/datalake_eurostat_index.json` with the following structure. Replace `<ES43_ID>`, `<ES61_ID>`, `<NATIONAL_ID>`, `<POP_ID>` with the actual ULIDs from Task 1 ingestion output.

```json
{
  "_meta": {
    "created": "2026-04-29",
    "description": "Maps (dataset|unit|geo_key) triples to data-lake source IDs for deterministic Eurostat lookup",
    "datalake_root": "/Users/guillem/vault/projects/personal/data-lake"
  },
  "nama_10r_2gdp|EUR_HAB|ES53": "01KPWPQ902XYG9BT12QCXKFG4K",
  "nama_10r_2gdp|EUR_HAB|ES43": "<ES43_ID>",
  "nama_10r_2gdp|EUR_HAB|ES61": "<ES61_ID>",
  "nama_10_pc|CLV10_EUR_HAB|B1GQ|AT|BE|DE|DK|EL|ES|FI|FR|IE|IT|LU|NL|PT|SE|UK": "<NATIONAL_ID>",
  "demo_pjan|T|TOTAL|AT|BE|DE|DK|EL|ES|FI|FR|IE|IT|LU|NL|PT|SE|UK": "<POP_ID>"
}
```

Key format rules:
- NUTS2 regional sources use `dataset|unit|geo` (single geo)
- Batched national sources list sorted geo codes separated by `|`
- All geo codes use Eurostat convention (EL for Greece, not GR)

Verify the index by checking that each source_id resolves to an existing `raw.json`:
```python
import json
from pathlib import Path
idx = json.loads(Path("scripts/datalake_eurostat_index.json").read_text())
root = Path(idx["_meta"]["datalake_root"])
for key, sid in idx.items():
    if key == "_meta": continue
    assert (root / "lake" / "sources" / sid / "raw.json").exists(), f"Missing: {key} -> {sid}"
print("All source IDs resolve to existing raw.json artifacts")
```
  </action>
  <verify>
    <automated>cd /Users/guillem/vault/projects/personal/ib-gdp-evolution && python3 -c "
import json; from pathlib import Path
idx = json.loads(Path('scripts/datalake_eurostat_index.json').read_text())
root = Path(idx['_meta']['datalake_root'])
count = 0
for key, sid in idx.items():
    if key == '_meta': continue
    assert (root / 'lake' / 'sources' / sid / 'raw.json').exists(), f'Missing: {key} -> {sid}'
    count += 1
assert count == 5, f'Expected 5 entries, got {count}'
print(f'OK: {count} index entries all resolve')
"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/datalake_eurostat_index.json` exists and is valid JSON
    - Contains exactly 5 data entries (3 NUTS2 + 1 national batch + 1 population batch) plus `_meta`
    - Key `nama_10r_2gdp|EUR_HAB|ES53` maps to `01KPWPQ902XYG9BT12QCXKFG4K`
    - Every source_id in the index resolves to an existing `raw.json` in the data-lake
    - `_meta.datalake_root` is `/Users/guillem/vault/projects/personal/data-lake`
  </acceptance_criteria>
  <done>Static catalog index maps all 5 (dataset|unit|geo) triples to verified data-lake source IDs</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Eurostat API → data-lake | Untrusted network response ingested as raw.json artifact |
| data-lake artifact → extend_gdp.py | Semi-trusted local file read by ETL pipeline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Spoofing | Eurostat API response | mitigate | HTTPS transport (Eurostat API is HTTPS-only); downstream sanity checks in Plan 03 validate data plausibility |
| T-05-02 | Tampering | data-lake raw.json | accept | Developer-controlled local filesystem; git-tracked index provides integrity reference; sanity checks catch implausible values |
| T-05-03 | Denial of Service | Eurostat API timeout | mitigate | Data-lake stores artifacts locally; adapter fallback in Plan 02 provides resilience; one-off ingestion so DoS is transient |
</threat_model>

<verification>
- All 4 data-lake source directories contain valid JSON-stat v2.0 `raw.json` files
- `scripts/datalake_eurostat_index.json` exists with 5 entries + `_meta`
- Every index entry resolves to an existing artifact on disk
</verification>

<success_criteria>
- 3 new Eurostat sources successfully ingested into data-lake (ES43, ES61 NUTS2 + national batch + population batch)
- Static catalog index file created with correct source ID mappings
- All source IDs verified against actual data-lake artifacts
</success_criteria>

<output>
After completion, create `.planning/phases/05-data-lake-ingestion-catalog-index/05-01-SUMMARY.md`
</output>
