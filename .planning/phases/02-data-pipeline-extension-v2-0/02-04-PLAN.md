---
phase: 02-data-pipeline-extension-v2-0
plan: 04
type: execute
wave: 4
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - scripts/extend_gdp.py
  - public/data/act2_balearic_islands.csv
  - public/data/act2_extremadura.csv
  - public/data/act2_galicia.csv
  - public/data/act2_castilla_la_mancha.csv
  - public/data/act2_portugal.csv
  - public/data/act2_ireland.csv
  - public/data/act2_malta.csv
  - public/data/act2_eu15_avg.csv
  - output/sanity_report_act2.txt
autonomous: true
requirements: [DATA-05, DATA-07, DATA-08]
user_setup: []
must_haves:
  truths:
    - "Each `public/data/act2_*.csv` has header `year,gdp_pc,source,unit` and matches `balearic_gdp_pc.csv` column semantics (D-08)"
    - "Sanity report has no FAIL; WARN only with explanation in report body (ROADMAP + DATA-07)"
    - "`public/data/balearic_gdp_pc.csv` is never overwritten (D-06, DATA-05 for Act II as separate act2 file)"
  artifacts:
    - path: "public/data/act2_*.csv"
      provides: "One file per series for parallel fetch in Act II (D-07)"
    - path: "output/sanity_report_act2.txt"
      provides: "Check listing same format as today"
  key_links:
    - from: "scripts/extend_gdp.py main/CLI"
      to: "public/data/act2_*.csv"
      via: "explicit write step after all series built"
      pattern: "act2_ + slug from CONTEXT"
---

<objective>
Emit all Act II per-series CSVs under `public/data/`, run extended sanity checks for every output series, and write the markdown/text sanity report. Satisfy DATA-07 (all checks) and DATA-05 (IB 2020 on Act II file). Resolve DATA-08 vs D-07: **deliver the locked layout** (one file per series, schema `year,gdp_pc,source,unit`); this supersedes the older REQUIREMENTS line about a single long CSV (note in SUMMARY for traceability).

Purpose: Produces the static assets Phase 3 will load; final gate before chart work.

Output: `act2_balearic_islands.csv`, `act2_extremadura.csv`, `act2_galicia.csv`, `act2_castilla_la_mancha.csv`, `act2_portugal.csv`, `act2_ireland.csv`, `act2_malta.csv`, `act2_eu15_avg.csv` (exact slugs per D-07—executor normalizes e.g. `castilla-la-mancha` vs `castilla_la_mancha` to one kebab or snake style and documents in SUMMARY), plus `output/sanity_report_act2.txt`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-data-pipeline-extension-v2-0/02-CONTEXT.md
@public/data/balearic_gdp_pc.csv
@scripts/extend_gdp.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: write_act2_csv outputs (D-07, D-08, DATA-05, DATA-08)</name>
  <files>scripts/extend_gdp.py, public/data/act2_*.csv</files>
  <behavior>
    - Each output file: columns `year,gdp_pc,source,unit` (same as Act I).
    - All series cover 1900–2024 with no gaps where pipeline guarantees completeness; otherwise checks catch gaps.
  </behavior>
  <action>For each Act II series (IB, 3× Spain peers, 3× countries, EU-15), write `public/data/act2_{slug}.csv`. Map `gdp_pc_2011ppp` (or final column name) to `gdp_pc` for public schema compatibility. **Never write** to `balearic_gdp_pc.csv`. Optional: write `act2_index.json` listing slugs and paths (CONTEXT discretion). Commit CSVs to git if policy allows generated data in repo (match existing `public/data` pattern). Unit string should match Act I (2011 PPP / chain-linked real per contextual series).</action>
  <verify>
    <automated>cd /Users/guillem/vault/projects/personal/ib-gdp-evolution && test -f public/data/act2_balearic_islands.csv && head -n1 public/data/act2_balearic_islands.csv | grep -q 'year,gdp_pc,source,unit'</automated>
  </verify>
  <done>All eight files exist with correct header; Act I `balearic_gdp_pc.csv` unchanged (verify with `git status` or diff in SUMMARY).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: run_checks for Act II: all series, 2020 seam, no FAIL (DATA-07)</name>
  <files>scripts/extend_gdp.py, output/sanity_report_act2.txt</files>
  <behavior>
    - `run_checks` (or `run_act2_checks`) iterates all series: seam at ANCHOR_YEAR, growth preservation, missing years, outlier, plausibility, unit guard.
    - Pipeline exits non-zero if any check status is FAIL (same as current seam hard-fail for continuity).
  </behavior>
  <action>Extend `run_checks` to accept multi-output frame or list of series. Adjust coverage check expectations (not “~260” regions—scope is Act II series). Keep YoY 2020–2021 exclusion in outlier check. `write_report` extended for Act II inputs (list RW path, INE path, Eurostat paths, pop paths). WARN requires one-line explanation in `details` (ROADMAP criterion 4/5).</action>
  <verify>
    <automated>cd /Users/guillem/vault/projects/personal/ib-gdp-evolution && python3 scripts/extend_gdp.py --help</automated>
  </verify>
  <done>Full pipeline run with all inputs: report shows no FAIL lines for checks 1–7; WARN count documented.</done>
</task>

<task type="auto">
  <name>Task 3: End-to-end verification command (regression command)</name>
  <files>scripts/extend_gdp.py</files>
  <action>Run `python3 scripts/extend_gdp.py` (with default workspace) from repo root. Grep `sanity_report_act2.txt` (or default report path) for `FAIL` — expect none. `wc -l` each `act2_*.csv` &gt; 0 lines beyond header. Add these commands to 02-04-SUMMARY for UAT. If Nyquist: MISSING — not required if manual grep suffices per project deferral of test infra.</action>
  <verify>
    <automated>cd /Users/guillem/vault/projects/personal/ib-gdp-evolution && python3 -m py_compile scripts/extend_gdp.py</automated>
  </verify>
  <note>After full data run, executor adds: `grep` sanity report for `: FAIL` (expect no matches) and `wc -l` on each act2 CSV.</note>
  <done>Documented automated smoke steps in SUMMARY; successful local run recorded.</done>
</task>

</tasks>

<threat_model>
## STRIDE
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | I | public CSV | mitigate | No user input in writer; only pipeline outputs |
| T-02-04-02 | T | `public/data` | accept | Static site data; public by design |
</threat_model>

<verification>Full `python3 scripts/extend_gdp.py` after all prior plans; eight CSVs; report free of FAIL.</verification>

<success_criteria>DATA-05, DATA-07, DATA-08 satisfied per locked CONTEXT: per-series Act II files, schema parity with Act I, full sanity pass.</success_criteria>

<output>After completion, create `02-04-SUMMARY.md`</output>
