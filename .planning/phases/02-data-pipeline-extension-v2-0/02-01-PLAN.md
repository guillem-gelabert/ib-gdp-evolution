---
phase: 02-data-pipeline-extension-v2-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/extend_gdp.py
autonomous: true
requirements: [DATA-01, DATA-02]
user_setup: []
must_haves:
  truths:
    - "ANCHOR_YEAR defaults to 2020 and is used by chain-linking and seam checks (not hardcoded 2022)"
    - "Roses–Wolf population can be read from the same RW Excel as GDP/cap (per D-01)"
    - "Series routing accepts NUTS2, NUTS0 country codes, and INE CCAA codes (per D-04)"
  artifacts:
    - path: "scripts/extend_gdp.py"
      provides: "load_roseswolf_population, parameterized anchor, series-key handling"
  key_links:
    - from: "load_roseswolf_population"
      to: "roseswolf_regionalgdp_v7.xlsx"
      via: "new loader matching RW workbook structure"
      pattern: "population column/sheet as discovered in xlsx"
---

<objective>
Establish Act II data-pipeline foundation: 2020 anchor, RW population loading for later EU-15 weighting, and extensible series identifiers (NUTS2, NUTS0, INE) so INE and country peers can plug in without a second pass of architectural changes.

Purpose: Unblocks all per-series work (Spanish regions, country peers, EU-15) with one consistent chain-link and seam model per D-09.

Output: Refactored `extend_gdp.py` with `load_roseswolf_population()`, parameterized anchor year in `compute_chainlinked_output` / `run_checks`, and clear extension points for non-NUTS2 IDs.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-data-pipeline-extension-v2-0/02-CONTEXT.md
@scripts/extend_gdp.py

<interfaces>
<!-- Current exports and constants executors will extend -->
```text
YEAR_MIN, YEAR_MAX, CHAIN_START, ANCHOR_YEAR, PRE_CHAIN_END
PipelineError, load_roseswolf, load_eurostat, load_correspondence
compute_chainlinked_output(rw, euro, correspondence, correspondence_used) -> (output, growth_compare, coverage, dropped_reasons)
run_checks(output, rw, growth_compare, coverage, dropped_reasons) -> list[CheckResult]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parameterize ANCHOR_YEAR and seam / chain-link (DATA-01)</name>
  <files>scripts/extend_gdp.py</files>
  <behavior>
    - With ANCHOR_YEAR=2020, seam check compares extended output to RW at year 2020 (not 2022).
    - `load_roseswolf` year filtering remains consistent: RW values at anchor year must exist for each chain-linked code used in Act II (or documented interpolation path).
    - `compute_chainlinked_output` uses the active anchor for rw_anchor, euro_anchor, and growth_index (no remaining literal 2022 except possibly comments).
  </behavior>
  <action>Per D-09: set default `ANCHOR_YEAR` to 2020. Add `--anchor-year` CLI (default 2020). Pass anchor into `compute_chainlinked_output` and `run_checks`. Replace all internal uses of 2022 with the variable. Update `main()` failure message for seam FAIL to reference the active anchor. Do not change Act I `public/data/balearic_gdp_pc.csv` (D-06).</action>
  <verify>
    <automated>cd /Users/guillem/vault/projects/personal/ib-gdp-evolution && python3 -c "import importlib.util; spec=importlib.util.spec_from_file_location('e','scripts/extend_gdp.py'); m=importlib.util.module_from_spec(spec); spec.loader.exec_module(m); assert m.ANCHOR_YEAR==2020"</automated>
  </verify>
  <done>Default anchor is 2020; module loads; no hardcoded 2022 in chain or seam logic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: load_roseswolf_population() (D-01, prep DATA-06)</name>
  <files>scripts/extend_gdp.py</files>
  <behavior>
    - Returns a tidy DataFrame with columns including nuts2_code (or same geo key as `load_roseswolf`), year, and population consistent with the same workbook.
    - Drops invalid rows; normalizes codes with `normalize_code`.
  </behavior>
  <action>Per D-01: add `load_roseswolf_population(path: Path) -> pd.DataFrame` that reads population from `roseswolf_regionalgdp_v7.xlsx` (same file as GDP/cap). Inspect the workbook structure (sheet/columns), reuse `normalize_columns` / `identify_code_column` patterns. Document in a short module comment which sheet/column was used. Discretion: separate function vs optional flag on `load_roseswolf` is allowed—pick the clearest maintainability.</action>
  <verify>
    <automated>cd /Users/guillem/vault/projects/personal/ib-gdp-evolution && python3 -c "from pathlib import Path; import importlib.util; spec=importlib.util.spec_from_file_location('e','scripts/extend_gdp.py'); m=importlib.util.module_from_spec(spec); spec.loader.exec_module(m); p=m.find_input_file(Path('.'),'roseswolf_regionalgdp_v7.xlsx'); df=m.load_roseswolf_population(p); assert not df.empty and 'year' in df.columns"</automated>
  </verify>
  <done>Population loader runs against the checked-in RW xlsx and returns non-empty tidy data.</done>
</task>

<task type="auto">
  <name>Task 3: Series identifier types and Eurostat/INE routing hooks (DATA-02)</name>
  <files>scripts/extend_gdp.py</files>
  <action>Per D-04/D-05: introduce a small config structure (e.g. dataclass or TypedDict) for Act II series: `key`, `scope` in {`nuts2`, `ine_ccaa`, `nuts0`}, `rw_code`, `ine_code` or `eurostat_geo`, and `institutional_source` (INE vs Eurostat). No need to implement all series in this plan—only types + helpers to normalize lookup keys so PT/IE/MT and ES43/ES11/ES42/ES53 are representable. Export or keep module-level for use in plans 02-02–02-04.</action>
  <verify>
    <automated>cd /Users/guillem/vault/projects/personal/ib-gdp-evolution && python3 -m py_compile scripts/extend_gdp.py</automated>
  </verify>
  <done>Config types exist; py_compile passes; next plans can list concrete series without further structural churn.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local files → script | Unvalidated xlsx/csv; parsing must not execute macros |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | I | `load_*` | mitigate | Pandas only; no `eval`/`exec`; file paths from CLI/workspace |
| T-02-01-02 | T | `extend_gdp.py` | accept | Local-only; no network in pipeline |
</threat_model>

<verification>After all tasks: `python3 -m py_compile scripts/extend_gdp.py` and the task-specific snippets pass.</verification>

<success_criteria>DATA-01 (configurable anchor, default 2020) and DATA-02 (non-NUTS2 identifiers representable) addressed; RW population available for EU-15 weighting in plan 02-03.</success_criteria>

<output>After completion, create `.planning/phases/02-data-pipeline-extension-v2-0/02-01-SUMMARY.md`</output>
