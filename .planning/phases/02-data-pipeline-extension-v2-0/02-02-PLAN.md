---
phase: 02-data-pipeline-extension-v2-0
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - scripts/extend_gdp.py
  - data/
  - public/data/
autonomous: true
requirements: [DATA-02, DATA-03, DATA-05]
user_setup:
  - service: INE
    why: "Combined Spain CCAA Excel (cid=1254736167628) is not fetched at runtime in Phase 2 (per deferred ideas)"
    dashboard_config:
      - task: "Download GDP and GDP per person 2000–2024, all CCAA, place in data/ or public/data/ with stable filename documented in script header"
        location: "https://www.ine.es (Regional Accounts, cid=1254736167628)"
must_haves:
  truths:
    - "INE combined Excel is parsed for ES53, ES43, ES11, ES42 with same chain-link method as Act I (RW 1900–1999 + INE 2000+)"
    - "IB 2020-anchor Act II file is produced; `balearic_gdp_pc.csv` is unchanged (D-06)"
  artifacts:
    - path: "scripts/extend_gdp.py"
      provides: "load_ine_excel, INE+RW chain for Spanish series; CSV emission in 02-04"
  key_links:
    - from: "load_ine_excel"
      to: "pre-downloaded INE xlsx"
      via: "find_input_file"
      pattern: "documented filename"
---

<objective>
Implement INE path for Spanish regions: add `load_ine_excel` per D-03, chain-link Balearic Islands and three peer CCAA (Extremadura, Galicia, Castilla–La Mancha) with RW 1900–1999 and INE chain-linked 2000+, anchor 2020. Deliver per-series dataframes ready for `act2_*.csv` emission (emission can be consolidated in 02-04 with other series).

Purpose: Fulfills DATA-03; DATA-05 for IB as 2020-anchored Act II artifact (new file) without touching Act I CSV (D-06).

Output: Tidy per-region series in memory, INE parser tested on the checked-in file, INE file path documented in script or README comment (no new markdown file unless user asks).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-data-pipeline-extension-v2-0/02-CONTEXT.md
@scripts/extend_gdp.py
@public/data/balearic_gdp_pc.csv
</context>

<tasks>

<task type="auto">
  <name>Task 1: load_ine_excel() for combined Spain regional file (D-03)</name>
  <files>scripts/extend_gdp.py</files>
  <action>Implement `load_ine_excel(path: Path) -> pd.DataFrame` reusing `normalize_columns`, `parse_number`, and the same year/value extraction patterns as the existing IB INE path if present elsewhere in the repo; if not, inspect the INE xlsx (user-preplaced) and parse chain-linked **GDP per person** 2000–2024 for all CCAA rows. Expose a filter `filter_ccaa(df, code: str)` or equivalent for ES53, ES43, ES11, ES42. Per D-04: use CCAA-level codes, not NUTS2 IDs in INE. Document expected input filename in `find_input_file` fallback list (e.g. `ine_spain_ccaa_gdp_pc.xlsx`—adjust to actual file on disk). Per deferred: do not add INE API fetch.</action>
  <verify>
    <automated>cd /Users/guillem/vault/projects/personal/ib-gdp-evolution && python3 -m py_compile scripts/extend_gdp.py</automated>
  </verify>
  <done>Function exists; when INE xlsx is present under data/ or public/data/, one test row per target CCAA can be read without error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Chain-link four Spanish NUTS2 series (ES53, ES43, ES11, ES42) and IB Act II 2020 anchor (DATA-03, DATA-05)</name>
  <files>scripts/extend_gdp.py</files>
  <behavior>
    - For each code, pre-2000 from RW, 2000+ from INE slice merged at CHAIN_START with same growth logic as `compute_chainlinked_output` (adapted for INE as euro-like slice where Eurostat is today).
    - 2020 anchor: extended value at 2020 matches RW at 2020 for seam (within tolerance).
  </behavior>
  <action>Wire RW + INE in one pipeline for Spanish regions. Reuse or refactor `compute_chainlinked_output` so “institutional” post-2000 can come from INE-filtered data instead of the single global Eurostat file when `series` is Spanish. Keep `enforce_eurostat_unit_guard` for Eurostat inputs only. Map INE to internal tidy `year, value` for chain step. If current architecture assumes one global `euro` df, refactor to per-series euro/ine frame or a merged multi-source table—minimal change that is correct. **Do not modify** `public/data/balearic_gdp_pc.csv`. Act II IB is written only as `act2_balearic_islands.csv` in 02-04 (or at end of this plan if 02-04 is writer-only). Per D-09 anchor 2020.</action>
  <verify>
    <automated>cd /Users/guillem/vault/projects/personal/ib-gdp-evolution && python3 -c "import importlib.util; from pathlib import Path; spec=importlib.util.spec_from_file_location('e','scripts/extend_gdp.py'); m=importlib.util.module_from_spec(spec); spec.loader.exec_module(m); print('ok')"</automated>
  </verify>
  <done>Four regional series (including IB) exist as structured outputs in the script’s Act II run path; seam check passes for 2020 when run with full inputs.</done>
</task>

<task type="auto">
  <name>Task 3: Blocker list and input filename resolution</name>
  <files>scripts/extend_gdp.py</files>
  <action>If the INE combined file is not yet in the repo, raise `PipelineError` with a clear message listing expected path(s) and the INE URL from 02-CONTEXT. Do not commit placeholder xlsx. Ensure `find_input_file` can locate the INE file once the user adds it under `data/` or `public/data/`.</action>
  <verify>
    <automated>cd /Users/guillem/vault/projects/personal/ib-gdp-evolution && python3 -m py_compile scripts/extend_gdp.py</automated>
  </verify>
  <done>Missing-INE case fails fast with actionable error text.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| INE xlsx → parser | User-supplied file; treat as data only |

## STRIDE
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | I | `load_ine_excel` | mitigate | Pandas `read_excel` only; validate expected columns before use |
| T-02-02-02 | E | `PipelineError` messages | accept | No secrets in messages |
</threat_model>

<verification>With all inputs present: run Act II entrypoint (or `main` with flags) and confirm no FAIL on seam for Spanish series—full verification in 02-04.</verification>

<success_criteria>DATA-02/03/05 advanced: Spanish peer + IB series chain-linked with INE; Act I file untouched by design (D-06).</success_criteria>

<output>After completion, create `02-02-SUMMARY.md`</output>
