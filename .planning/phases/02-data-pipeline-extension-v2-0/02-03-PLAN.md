---
phase: 02-data-pipeline-extension-v2-0
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - scripts/extend_gdp.py
  - data/
  - public/data/
autonomous: true
requirements: [DATA-04, DATA-06]
user_setup: []
must_haves:
  truths:
    - "Portugal, Ireland, Malta: RW 1900–1999 + Eurostat CLV 2000+; CLV unit guard applied (D-05)"
    - "EU-15: sum(pcpi_i × pop_i) / sum(pop_i) with RW pop pre-2000 and Eurostat pop post-2000 (D-01, D-02, act2 notes §5)"
  artifacts:
    - path: "scripts/extend_gdp.py"
      provides: "country peer chain-linking, EU-15 aggregate series"
  key_links:
    - from: "EU-15 row"
      to: "per-country pcpi and pop from RW + Eurostat"
      via: "year-aligned merge and weighted sum"
      pattern: "align with EU-15 list in act2-datastory-decisions.md"
---

<objective>
Add Eurostat NUTS0 chain-linked series for PT, IE, and Malta (DATA-04). Build the EU-15 reference as a population-weighted average of per-capita GDP for the 15 member countries in `act2-datastory-decisions.md` (BE, DK, DE, IE, GR, ES, FR, IT, LU, NL, AT, PT, FI, SE, UK), using `load_roseswolf_population` for pre-2000 and Eurostat `demo_r_d2jan` (or `demo_pjan` national) for post-2000 population (D-01/D-02). Interpolate RW decadal steps for annual EU-15 weighting per CONTEXT discretion; document the choice (linear vs step) in code comment and SUMMARY.

Purpose: Produces peer country series and the EU-15 benchmark for Act II charts.

Output: In-memory (and ready for 02-04 file emission) `act2_portugal`, `act2_ireland`, `act2_malta`, and `act2_eu15_avg` series.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-data-pipeline-extension-v2-0/02-CONTEXT.md
@.planning/notes/act2-datastory-decisions.md
@scripts/extend_gdp.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NUTS0 chain-link for PT, IE, MT (DATA-04)</name>
  <files>scripts/extend_gdp.py</files>
  <behavior>
    - Each of PT, IE, MT has full year range 1900–2024 in output (after join), subject to data availability; gaps surface as check WARN/ FAIL per existing rules.
    - `enforce_eurostat_unit_guard` runs on every Eurostat CSV read for these series.
  </behavior>
  <action>Per D-05: use Eurostat `nama_10r_2gdp` (or `nama_10_gdp`) extract filtered to NUTS0 `geo` PT, IE, MT with CLV per-capita unit. Reuse the same `compute_chainlinked_output` pattern with `rw` rows for `nuts2_code` = PT, IE, MT (Roses–Wolf uses these labels per peer table in CONTEXT). If the global `eurostat_nama_10r_2gdp.csv` already contains all countries, filter; otherwise allow extra `--eurostat-path` discovery or additional input file from `data/`. **Do not** use current-price or PPS (guard must pass).</action>
  <verify>
    <automated>cd /Users/guillem/vault/projects/personal/ib-gdp-evolution && python3 -m py_compile scripts/extend_gdp.py</automated>
  </verify>
  <done>Three country series computed with CLV guard passing on inputs used.</done>
</task>

<task type="auto">
  <name>Task 2: Eurostat population loader for 2000+ (D-02)</name>
  <files>scripts/extend_gdp.py</files>
  <action>Add `load_eurostat_population` (or similar) to parse a pre-downloaded `demo_r_d2jan` / national demo CSV for annual population by `geo` for EU-15 countries. Place expected filename(s) under `find_input_file` (e.g. `eurostat_demo_pop_nuts0.csv`—executor picks actual). Align years with chain-linked GDP for weighting. If a country lacks a year, document WARN in report.</action>
  <verify>
    <automated>cd /Users/guillem/vault/projects/personal/ib-gdp-evolution && python3 -m py_compile scripts/extend_gdp.py</automated>
  </verify>
  <done>Population series available per country for years overlapping EU GDP/cap 2000+.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: EU-15 weighted reference series (DATA-06)</name>
  <files>scripts/extend_gdp.py</files>
  <behavior>
    - For each year, EU-15 = sum(pcpi_i × pop_i) / sum(pop_i) over the 15 countries.
    - Same anchor year 2020 as other series; seam continuity where RW meets Eurostat for each country propagates to aggregate.
  </behavior>
  <action>Implement per-country chain-linked pcpi (reuse Task 1 pattern for all 15 NUTS0 codes in the decisions file—not MT-only peers; UK = GB/UK per Eurostat `geo` label in file). For each year 1900–1999, interpolate RW decadal pcpi to annual (same as existing roses_wolf / regional logic). Combine with RW population from `load_roseswolf_population` for pre-2000; Eurostat pop for 2000+. At 2000 boundary, use one consistent population source rule (document if small seam). Output one tidy series for EU-15. Discretion: linear interpolation between RW decadal points is preferred unless the existing regional code uses step—**match the existing regional behavior** for consistency.</action>
  <verify>
    <automated>cd /Users/guillem/vault/projects/personal/ib-gdp-evolution && python3 -c "import importlib.util; spec=importlib.util.spec_from_file_location('e','scripts/extend_gdp.py'); m=importlib.util.module_from_spec(spec); spec.loader.exec_module(m); print('ok')"</automated>
  </verify>
  <done>EU-15 series array available for write step; formula documented in code comment (reference act2 notes §5).</done>
</task>

</tasks>

<threat_model>
## STRIDE
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | I | Eurostat pop CSV | mitigate | Same read-only parse as `load_eurostat` |
| T-02-03-02 | R | weighting | mitigate | Assert non-negative pop; skip year if invalid with WARN |
</threat_model>

<verification>Cross-check: EU-15 2020 value is finite; sum of weights &gt; 0 for a test year.</verification>

<success_criteria>DATA-04 and DATA-06 implemented: three peers + EU-15 reference in pipeline.</success_criteria>

<output>After completion, create `02-03-SUMMARY.md`</output>
